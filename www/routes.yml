/:
  controller: general
  action: home

/me-contacter:
  controller: general
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/dashboard:
  controller: admin
  action: dashboard

/users:
  controller: admin
  action: getUserList

/users/{id}:
  controller: user
  action: show

/users/{id}/delete:
  controller: user
  action: delete

/users/{id}/update:
  controller: user
  action: update
